# Moved — use `new ui/`

This React app was the **legacy** web dashboard. It has been archived to:

`old_versions/react-app_webviewer_legacy/`

The active UI is **`new ui/`** at the repository root:

- Theme 1 (Classic) + Theme 2 (Modern)
- Tooltips, 3D viewport, C++ API bridge on `http://127.0.0.1:8000`

Build the desktop app with `-DSZM_USE_REACT_WEBVIEW=ON` (default for full desktop).
